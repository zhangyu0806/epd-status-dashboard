# EPD uploader 环境配置（复制到 /etc/epd-uploader.env）
#
# 树莓派一体机（同机渲染 + 传屏）：指向本机 HTTP
EPD_IMAGE_URL=http://127.0.0.1:8088/status.png
#
# 树莓派/小主机作蓝牙网关（图在云服务器上渲染）：指向云服务器
# EPD_IMAGE_URL=http://YOUR_SERVER_IP:8088/status.png
#
# 上传/刷新间隔秒数（最小 600，避免频繁刷屏伤墨水屏）
EPD_INTERVAL_SECONDS=600
